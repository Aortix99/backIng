# --- PostgreSQL (Supabase u otro): preferido ---
# Si la contraseña tiene @ u otros caracteres especiales, codifícalos en la URL (ej. @ -> %40)
# DATABASE_URL=postgresql://postgres:TU_PASSWORD@db.xxx.supabase.co:5432/postgres

# --- MySQL local (solo si NO usas DATABASE_URL) ---
# DB_NAME=ing_civil_db
# DB_USER=root
# DB_PASSWORD=password
# DB_HOST=localhost
# DB_PORT=3306

NODE_ENV=development
PORT=5000

JWT_SECRET=cambia_esto_en_produccion
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# CORS (origen del front Angular)
# FRONTEND_URL=http://localhost:4200
